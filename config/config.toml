ServiceHost = "0.0.0.0"
ServicePort = 8000
Token = "secret_token"

[minio]
Endpoint = "localhost:9000"
BucketName = "images"

[jwt]
Token = "maxos"
ExpiresIn = "24h"

[Redis]
DialTimeout = "10s"
ReadTimeout = "10s"